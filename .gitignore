node_modules
.next
out
.DS_Store
*.log
.env*.local
next-env.d.ts
Screenshots/

# /friday easter-egg media is hosted on a CDN (see NEXT_PUBLIC_FRIDAY_* env
# vars); keep the heavy local capture files out of the repo.
public/friday/clip.json
public/friday/audio.webm

# /birthday easter-egg media (see NEXT_PUBLIC_BIRTHDAY_* env vars); same deal.
public/birthday/clip.json
public/birthday/audio.webm
